/**
 * Plugin Super Class
 *
 * All plugins will inherit this plugin functionality and extend it
 *
 * @param layer
 * @constructor
 */
function Plugin(layer) {

    var _this = this;

    /**
     * @type {Layer}
     */
    this.layer = layer;

    function getLayer(query) {
        //mark component as used
    }

    function getIntegration() {

        //get unused components
        //process the integration json
        //inject in current response

    }

}

module.exports = Plugin